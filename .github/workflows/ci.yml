name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm -r typecheck

      - name: Unit tests
        run: pnpm test

      - name: Cross-tenant isolation suite
        run: pnpm exec vitest run packages/backend/src/isolation.test.ts

      - name: Build widget
        run: pnpm --filter @platform/widget build

      - name: Widget size budget (<=30KB gz)
        run: pnpm widget:size

      - name: Build marketing site
        run: pnpm --filter @platform/marketing build

      - name: Build portal
        run: pnpm --filter @platform/dashboard build
        env:
          VITE_API_BASE: https://chatbot-api-dev.example.com
          VITE_USER_POOL_ID: us-east-1_placeholder
          VITE_USER_POOL_CLIENT_ID: placeholder

      - name: CDK synth
        env:
          DOMAIN_NAME: ${{ vars.DOMAIN_NAME || 'example.com' }}
          ENV: dev
          AWS_REGION: us-east-1
          CDK_DEFAULT_ACCOUNT: "000000000000"
          # Stub zone id so synth uses fromHostedZoneAttributes (no AWS creds/network).
          HOSTED_ZONE_ID: ${{ vars.HOSTED_ZONE_ID_DEV || 'ZSYNTHSTUB0000' }}
        run: pnpm synth

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build widget
        run: pnpm --filter @platform/widget build

      - name: Build portal (E2E)
        run: pnpm --filter @platform/dashboard build
        env:
          VITE_E2E: "1"
          VITE_API_BASE: http://localhost:4500
          VITE_USER_POOL_ID: us-east-1_e2estub
          VITE_USER_POOL_CLIENT_ID: e2eclient123

      - name: Install Playwright browser
        run: pnpm --filter @platform/e2e exec playwright install --with-deps chromium

      - name: Widget handshake E2E
        run: pnpm --filter @platform/e2e exec playwright test

      - name: Money-path E2E (signup→key→embed→answer→transcript)
        run: pnpm --filter @platform/e2e exec playwright test --config money.config.ts
