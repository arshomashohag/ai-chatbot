{
  "name": "@platform/widget",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.chat.config.ts",
    "build": "vite build && vite build --config vite.chat.config.ts",
    "build:loader": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@platform/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
